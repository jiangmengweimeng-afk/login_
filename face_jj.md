- 定义 def get_stock:
- try:
- 尝试网络请求获取数据
- 如果成功 则备份文件
- except Exception as e:
- 如果不是重新开始请求网络三次 每次间隔三秒 if循环 判断请求次数是否为三秒
- 若其中某次请求成功获取数据 则备份文件
- 若是三次请求都失败 则检查本地文件 倘若本地文件有符合要求即可以备份 倘若本地文件不符合要求直接彻底失败 ，捕获RunTimeError 时提示跳过改股票